14-2-11		通用的can总线还没有完成
14-2-12		can1的发，收都正常
			can2不正常
			之后又出现了can1也不正常的情况，不知道是不是盒子的问题。
			栋栋发现是线的问题，can2可以发但是不能收

2014-3-23	蜂鸣器响是在systick里设置的，在空闲线程里对一个变量累加，
			在systick中判断该值是否累加，如果累加则对该变量清零。如果
			没有累加，即空闲进程没有得到运行，那么蜂鸣器就会响。
			会出现这种状况：
			can总线发送失败
			有死循环
			
2014-3-25	led指示灯翻转  fxh_bool 3-25 20:07
			---------------------------------------------------------
			|	led1	|	system run OK  SysTick_Handler()		|
			---------------------------------------------------------
			|	led2	|	uart dma receive OK						|
			---------------------------------------------------------
			|	led3	|	can receive OK							|
			---------------------------------------------------------
			|	led4	|	用户自定义								|
			---------------------------------------------------------
			
2014-4-9   取消CAN自动重传功能 by fxh_bool  (后来由于发送丢帧问题又加了上去)